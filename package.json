{
  "name": "utilipea-monorepo",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "update-deps": "npx npm-check-updates -ws --root --format group -i",
    "generate": "bun run ./docs/.vitepress/index.ts"
  },
  "dependencies": {
    "typescript": "^5.4.4",
    "vitepress": "^1.0.2"
  },
  "devDependencies": {
    "bun-types": "latest",
    "scule": "^1.3.0"
  }
}
