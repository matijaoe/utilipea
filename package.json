{
  "name": "utilipea-monorepo",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "prepare": "husky install"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "vitepress": "^1.0.0-rc.32"
  },
  "devDependencies": {
    "bun-types": "latest",
    "husky": "^8.0.3",
    "scule": "^1.1.1",
    "type-fest": "^4.8.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "echo 'Running pre-commit hook' && npm run lint && npm run test"
    }
  }
}
